# 审计 · 2026-08-16

一次会话内做完的排查,全部基于实测(截图 / 几何测量 / 对比度计算 / 浏览器内探针),
不是读代码推断。每条都附了复现方式,方便下一个人自己验一遍。

> `_redirects` 已把 `PROGRESS.md` 挡在公网外。本文件同类,请一并挡掉或保持私有。

---

## 1 · 纹理为什么每次都"风格不和"(根因,不是审美问题)

**六次尝试全部失败,原因是结构性的,和品味无关。**

8-10 到 8-15 之间纹理系统被推翻了六轮:纸纹 → 强化 → 关闭 → 宝相花 → 莲芡 → 退役 → 再修。
每一轮都在调"画什么、多深、铺多密",但没人动过真正坏掉的那一层。

### 1.1 颜色被焊死在文件里

```
editorial-fiber-grid.svg    stroke="#141413"
map-graticule.svg           stroke="#8C7A5E"
paper-foxing.svg            <rect fill="#EBDDC2">   ← 不透明底
engraving-hatch.svg         <rect fill="#F0E8D6">   ← 不透明底
```

站点背景每 15 秒在 **180 组配色**里轮换(`mountain-range.js` / `paletteDwellMs`)。
而通过 CSS `url()` 加载的 SVG **看不到页面的 CSS 变量** —— 这是浏览器的硬限制,
不是配置问题。

于是:纹理的颜色是为某一组配色挑的,**剩下 179 组它都是个外来户**。

后两个更糟 —— 它们带不透明背景矩形,所以不是"叠"在背景上,是**在活的配色上凿一个死色的洞**。
无论怎么调不透明度都不可能和谐。

### 1.2 可行的做法:把 SVG 当镂空模板,不当图片

```css
.tex{
  -webkit-mask-image: url("../assets/textures/editorial-fiber-grid.svg");
          mask-image: url("../assets/textures/editorial-fiber-grid.svg");
  mask-size: 128px 128px; mask-repeat: repeat;
  background-color: color-mix(in oklab, var(--bw-palette-gem) 40%, transparent);
}
```

`background-image` 换成 `mask-image`,颜色改由 `background-color` 提供。
手绘的形状**一笔都不用重画**,只是颜色来源从文件内部换成了活的色组 token,
于是纹理自动跟着背景转 —— 结构上不可能再撞色。

已用 5 组跨度很大的真实配色验证过。

前提:带不透明背景矩形的两个文件要先删掉那行 `<rect fill="…">`,
否则蒙版是实心的,遮不出形状。

### 1.3 其他两条路(按场景选)

| 方案 | 适合 | 代价 |
|---|---|---|
| **A. 纯 CSS 渐变**(`repeating-linear-gradient` + `color-mix`) | 网格、斜纹、graticule 这类几何纹理 | 零请求、随分辨率永远锐利;画不了手绘感 |
| **B. 内联 `<svg>` + `<pattern fill="currentColor">`** | 需要手绘笔触、且只用在一两个地方 | 继承 `color`,但要写进 HTML |
| **C. `mask-image`(上面这个)** | **现有资产的最省迁移** | 保留全部手绘形状,只换颜色来源 |

---

## 2 · 首页"空"的实情

`tokens/paper.css` 里那五行纹理引用**全是注释**:

```
44:  /* assets/textures/paper-foxing.svg     site-wide body background  */
45:  /* assets/textures/map-graticule.svg    chat thread, empty states  */
…
```

**首页目前一层纹理都没有**,可见图层只有 `.mtn-bg`(山脉 SVG,opacity .82)。
所以"空"不是纹理太淡,是根本没上。

---

## 3 · 4.3 MB 的孤儿资产(仍在部署)

八个 PNG 无人引用,是被放弃的宝相花实验:

```
baoxianghua-editorial-field.png          1098.8 KB
09-beaded-medallion.png                   675.6 KB
08-cloud-medallion.png                    574.6 KB
04-scroll-rosette.png                     491.9 KB
07-petal-wheel.png                        448.8 KB
05-layered-rosette.png                    419.1 KB
06-simple-rosette.png                     415.0 KB
baoxianghua-micro-monogram.png            149.7 KB
```

复现:`grep -rl <文件名> --include=*.{html,css,js} .` 全部返回 0 个文件。

另有 `assets/palettes/color-groups-180.json`(68 KB)也无人引用 —— 实际读的是
`color-groups.json`。

---

## 4 · Box 的毛边

### 4.1 52 种圆角

```
999px×27  8px×11  11px×9  5px×8  18px×8  10px×8  6px×7  20px×6
12px×6  9px×5  2px×5  17px×5  7px×4  24px×3  16px×3 …
```

2px 到 28px 之间几乎每个整数都被用过。设计系统本来只该有两档
(`--ui-radius` / `--ui-radius-small`),但:

```
border-radius 用 var() :  10 处
写死数值      :         173 处
→ token 覆盖率 5%
```

后果不是某一处难看,是**并排的盒子圆角差 1–2px,说不出哪里不对,但整体显糙**。

### 4.2 13 处小数像素边框

```
index.html:259   border-bottom: 1.5px
index.html:385   border: 1.4px
index.html:393   border: 1.6px
refinement.css   border: 1.25px  ×4
refinement.css   border: .75px
```

**1x 屏上小数边框必然被抗锯齿糊成灰线** —— 这是字面意义的毛边。
而且 1.4 / 1.5 / 1.6 混用,同一页里线的粗细不一致。边框宽度一共 8 种。

### 4.3 一个真实的 padding 缺陷

`guide.html` 并排两张 `.lesson-card`(浏览器实测计算值):

```
左卡  bg=透明        padding = 22 / 51.2 / 22 / 0
右卡  bg=半透明橄榄   padding = 22 / 0 / 22 / 51.2    ← padding-right: 0
```

带底色的那张**右内边距是 0**,任何排满的一行都会直接贴到色块边缘。
原意是做栏间沟(gutter),但一旦其中一栏上了底色,0 padding 的那侧就露馅。
现在看着还行,只是因为那两行正好断得早 —— 这是潜伏缺陷,不是偶发。

---

## 5 · 对比度不可控(最根本的一条)

`pricing.html` 实测 **35 处文字对比度不达标**:

```
1.42:1  "Monthly" / "Annually"   13px    需 4.5
1.42:1  "Free"                   22px    需 3
3.36:1  导航 / 正文 / 套餐说明     大量     需 4.5
```

`1.42:1` 基本等于看不见。

但真正的问题不是"这几个颜色调错了" —— 是 **180 组配色里没有任何一组做过对比度校验**。
同一段文字在这组勉强能读,下一组可能就废了。**现在没有对比度保证,只有运气。**

唯一的根治办法:给 180 组跑一遍校验,不达标的要么调整、要么排除出轮换池。
这是可以脚本化、一次做完的。

---

## 6 · 文字页排版

- **行宽超标**:`guide.html` 引用块 **84 CPL**(710px @ 17px)。舒适区 45–75,
  超过 80 眼睛回扫找行首会变吃力。
- **双栏基线错位**:`terms.html` 目录第 05 条折成两行,右边第 06 条被垂直居中到
  05 的两行之间,基线和 05 的任何一行都不对齐。十条错一条,整块就显得没对齐。
- **双栏长度不平衡**:`terms.html` 左栏在日期就结束,右栏还有一大截,左下约 300px 空白。
- **纹理压正文**:`guide.html` 的菱形格纹在当前配色下对比度相当高,明暗跨度和正文灰度
  同一量级,会实打实拖慢阅读。

---

## 7 · 其他粗糙细节

- **两个引导气泡同时弹**:首屏右上「How does this work?」+ 左下「First reading?…」,
  新用户第一眼要同时处理两个打断。
- **纹理带硬切**:`terms.html` 底部的纹理带凭空开始,上沿一条硬边,不淡入也不对齐任何东西。
- **顶部无意义灰带**:`terms.html` y=0–80 露出页面底色,和内容纸面之间一条硬边。
- **次要按钮弱到近乎消失**:`guide.html` 底部的「Back」。
- **定价页中段约 350px 纯空白**:hero 文案到余额条之间。转化页上这是很贵的空白。

---

## 8 · 首屏 JS 约 290 KB(未压缩)

```
casting-figure.js  85 KB      chat-app.js     84 KB
liuyao-engine.js   30 KB      liuyao-ai.js    22 KB
prompt-router.js   21 KB      liuyao-chart.js 20 KB
```

`casting-figure.js` 的绝大部分是摇卦动画,而**空状态根本不摇卦** ——
完全可以等用户真的提问再加载。

---

## 9 · 一个共同的根

圆角、边框粗细、内边距、纹理强度、文字颜色 —— 每一样都有 token,
但 **95% 的地方绕过 token 直接写数值**。

三个会话并行五天、十个 HTML 各改 40+ 次,每个人都在自己那块凭手感填数,
于是长出 52 种圆角和 8 种边框。

**这不是审美问题,是流程问题。** 单独修任何一处,都会被下一次改动重新弄乱。
修复顺序应该是:先收敛原子(边框、圆角),再加规则挡住新的裸数值,
否则一周后又是 52 种。

分支与同步方面的问题另见 PR #57 写入 `CLAUDE.md` §7.1–7.3。
