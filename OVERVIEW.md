# OVERVIEW — BourneWise 是个什么程序

> 三份文档的分工:
> **本文**回答「这是什么、怎么运转、为什么这么造」——给第一次接触这个仓库的人。
> `ARCHITECTURE.md` 管仓库(分支、权属、协作协议)。
> `CLAUDE.md` 管代码(设计法则、语气、性能红线)。
>
> 本文所有数字都是 2026-08-21 在代码上实测的,不是从别的文档抄的。

---

## 1 · 一句话

**六爻起卦是确定性程序算的,解读是大模型写的 —— 两件事故意分开,而且分给用户看。**

用户问一个问题 → 程序摇出一个卦(六爻、变爻、变卦、纳甲、六亲、六神,全部可复算)→
把这个**已经定死的盘面**连同问题交给 Claude → 模型只负责解释它拿到的东西,**不参与决定卦是什么**。

这条分界是产品的全部立身之处。占卜类 AI 产品的通病是「模型顺着你的话编一个卦」,
在这里**做不到**:卦在模型被调用之前就算完了,而且盘面渲染在页面上,用户能自己对。

---

## 2 · 技术规格

### 形态:没有构建步骤的静态站 + 边缘函数

```
浏览器 ──静态 HTML/CSS/JS──▶ Cloudflare Pages
   │
   └── /api/* ──▶ Cloudflare Pages Functions ──▶ OpenRouter → Claude
                        │
                        └──▶ D1 (SQLite) 账户/点数/历史
```

| 项 | 规格 | 为什么 |
|---|---|---|
| 前端 | **原生 HTML/CSS/JS,零框架、零构建** | 改一个字就是改那个文件,没有编译产物,没有 source map 对不上 |
| 运行时依赖 | **0 个** | `package.json` 里只有一个 devDependency(`wrangler`),`node_modules` 不参与部署 |
| 后端 | Cloudflare Pages Functions(10 个端点) | 和静态资源同源同仓,不需要单独的服务 |
| 数据库 | Cloudflare D1(SQLite),6 张表 | `users` `ledger` `castings` `rate_limits` `billing_events` `subscriptions` |
| 模型 | Claude,经 **OpenRouter** 调用 | key 只在服务端;浏览器只声明**意图**(`product`/`role`),模型由服务端选 |
| 支付 | **Creem**(不是 Stripe) | 订阅 + 一次性点数包,webhook 回写 D1 |
| 认证 | 邮箱密码 + OAuth(Google / Reddit / Discord)+ Apple | 未配置 key 的按钮**自动隐藏**,不是留个死链接 |
| 部署 | push `main` → Cloudflare 自动部署 | 无 CI 构建;PR 自动生成预览部署 |

### 体量(实测)

| | 文件数 | 行数 |
|---|---|---|
| JS | 29 | 12,387 |
| JSON(主要是色卡) | 7 | 12,866 |
| MD(文档) | 9 | 4,534 |
| HTML | 14 | 3,746 |
| CSS | 13 | 3,642 |
| 测试(`.mjs`) | 17 | — |

关键路径 **230KB**(原 290KB;`casting-figure.js` 85KB 移出首屏后)。

### 为什么是这套

**「没有构建步骤」是一条产品决策,不是偷懒。** 这个站的视觉是一套活的系统 ——
背景 114 组配色在跑、图案是运行时生成的 SVG、玻璃材质靠实时合成。
**这些东西没法在构建期烘焙**,烘焙了就变成 CLAUDE.md 里反复记录的那个错误:
颜色被写死进图片,然后在某几十组配色下必然撞色。
所以资产层的规则是:**能算出来的就不要存成文件。**

---

## 3 · 三个子系统

### ① 起卦引擎 `liuyao-engine.js`(574 行,纯函数)

`computeBoard({ lines, changeIdx, date })` → 完整盘面。

里面是**查表 + 位运算**,没有随机、没有网络、没有模型:
天干地支、五行生克、纳甲(内外卦分别配干支)、六亲、六神(按日干起)、
八宫世应(本宫/一世…游魂/归魂)、月建日辰旺衰。

**同样的输入永远得到同样的盘。** 这是「用户能自己对」的技术前提。

### ② 解读引擎 `functions/_lib/prompt-engine.js`(1,958 行,**服务端**)

⚠️ **08-14 安全审计后移到服务端。** 提示词栈就是产品本身,放在浏览器里等于开源它。
`_redirects` 把 `copywriting/*`(它的生成物)整个 404,`tests/prompt-secrecy.mjs` 守着不泄漏。

它是**分段拼装**的,不是一整块提示词。段名可见:`stance` `voice` `iron_laws`
`inference_traps` `clarity_rules` `density` `anti_failure` `crisis` `intimacy`
`appearance` `future_unseen` `grade` …

**架构上最要紧的一条**(CLAUDE.md §6 开头):
`stance`(先有自己的看法)排在 `voice` 之前,因为**其余所有段都是过滤器,只会做减法** ——
它们能拦住坏句子,产不出好句子。少了 `stance`,管道就是「盘面事实 → 约束 → 文字」,
出来的必然是满足了每条规则、而中心是空的东西。

调用链:`prompt-router.js`(浏览器,只声明意图)→ `/api/claude` → OpenRouter → Claude,
SSE 流式回传。OpenRouter 吐的是 OpenAI 形状的 `choices[].delta.content`,
函数把它**重新编码成 Anthropic 的 `content_block_delta`**,因为客户端已经在解析那个格式。

### ③ 背景引擎 `assets/backgrounds/mountain-range.js`(32KB)

10 层视差山脊 + 天空 + 水面,**114 组配色**每 15 秒整体交叉渐变,整轮 28.5 分钟。

- **色相从不改动**,明度和饱和度被 `mutedHex()` 夹住。实测:色卡平均饱和 .599 → 上屏 .242,
  84% 被压到上限 .26。
- **顺序是每位访客各洗一次的随机序**,种子存 `sessionStorage`,
  所以**会话内跨页面不重排,访客之间各不相同**。
- **摩尔纹是全站唯一的纹理**,不是贴图 —— 是每层自己的等高线在极小角度上重复、
  裁在该层山形里,所以它跟着那层漂移、继承那层的颜色、到天际线为止。
- **性能红线**:绝不逐帧动画 `background-color` / `filter` / `backdrop-filter`。
  只动 transform 和 opacity,配色靠 hold-then-crossfade。

---

## 4 · 一次解读实际发生什么

```
1. 用户写问题
2. 浏览器摇卦 → liuyao-engine 算出完整盘面(此时还没碰模型)
3. casting-figure.js 画排卦动画:十二笔踩同一条 620ms 心跳,落定即静止
4. prompt-router 声明意图 {product: stria|sortis, role} → POST /api/claude
5. 服务端:验会话 → 查余额 → 拼提示词 → 调 OpenRouter
6. SSE 流式回写,边下边渲染
7. 写完之后按实际 token 结算扣点
```

**第 5 步不预留、不设上限。** 第 7 步才扣,而且**扣多少是实际用量算出来的**。

---

## 5 · 计费模型(这是最反直觉的一块)

> **用多少扣多少。不设预留、不设上限、不设封顶结算。**

- **入场只验余额 > 0。** 不许按「典型成本」挡人 —— 上限必须设在典型用量之上,
  于是入场就得先要那一整笔,结果是把付得起的用户挡在门外。
- **开始了的解读一定读完。** 结算全额扣,**余额可以扣成负数**;
  被拒的是**下一次**请求,不是正在写的这一篇。`chargeUnits()` 因此**故意不带余额守卫**。
- 单价:`UNIT_PRICE_USD = 1/1500`,即 **1 美元 = 1,500 点**,订阅和点数包同价。
- `METHOD_COST` / `FOLLOW_COST` 只是**展示用估值**,不参与任何判断。

⚠️ 这条被一个 codex 会话加回过 reserve+cap 一次,导致新用户一卦都起不了。**改这里前先问 owner。**

---

## 6 · 设计思路

### 两套材质,按路由分,不许混

| | 玻璃路由(index / pricing / login / settings / 法务) | 色块路由(about / guide) |
|---|---|---|
| 载体 | 浮起来,半透明,背后透山脉 | 齐平贴边,实色不透明 |
| 圆角 | `--ui-radius` 系列 | **直角,一律 0** |
| 层级来自 | 光(高光边 + 极轻投影) | **相邻**(3px 纸缝分开两块颜色) |

**分界不是审美偏好,是物理**:色块路由把山脉关掉,背后什么都没有,
玻璃没有东西可折射 —— 在那儿用玻璃只会得到一块糊在纸上的灰。

### 三条贯穿全站的规则

1. **确定性,不是随机。** 笔触振幅按 `index mod 3` 步进、等高线漂移周期 5 和 7 互质、
   配色洗牌种子化 —— **同一个元素每次都画得一模一样**。
   逐次随机会在每次导航时重排,**读起来是渲染故障,不是手感**。
2. **比例就是母题。** 所有几何物料由参数生成,所以**参数写错 = 换了个母题**,
   靠调不透明度或尺寸改不回来。
3. **能算出来的不要存成文件。** 存成文件的颜色不会跟着 114 组配色走。

### 字体只有三种,字号只有三条梯

BioRhyme(可变字重 200–800,单文件)/ Spinnaker / Pacifico,全部自托管,不许外链。

| 字体 | 用途 | 梯级 |
|---|---|---|
| Spinnaker | UI / 标签 / 按钮 | 10 · 11 · 12 · 13 · 15 |
| BioRhyme | 标题 + 长文阅读 | 13 · 15 · 16.5 · 18 + hero 的 `clamp()` |
| Pacifico | 字标 + method chip **仅此二处** | 13 · 18 |

**半像素不是层级,是漂移。** 收口前全站 35 档、9–18px 之间挤了 17 档。

### 文字灰阶只有三级

`--ink` → `--dim` → `--faint`。原来的 `--faint`/`--ghost` 在玻璃上实测 3.7:1 / 2.4:1,
**全部配色都不达标**,而它们承载的是投卦免责声明、余额说明这些真正文。
**读不了的层级不是层级,是丢失的文字。**

---

## 7 · 质量闸门

**17 条契约测试**,全部必须绿:

| 测试 | 守什么 |
|---|---|
| `build-tag` | 全站 `?v=` 与 `version.json` 同值 —— 漏 bump 会让用户看到旧缓存,是历史上最常见的「改了没生效」 |
| `palette-contract` | 114 组结构、段是连续 id 区间、洗牌必须种子化且持久化 |
| `billing-contract` | 计费不许出现预留/上限 |
| `prompt-secrecy` | 提示词不泄漏到客户端 |
| `language-purity` | 英文解读里不出现汉字 |
| `token-cap` / `rates-contract` / `request-contract` / `session-contract` / `stream-recovery` / `upstream-error` | 后端契约 |
| `texture-contract` / `followup-axes` / `board-distill` / `xiang-trace` / `free-reading-contract` / `prompt-coverage` | 功能契约 |

另有 `tools/palette-contrast.html`:跑全部配色,输出每个文字角色的最差对比度与面板色偏。

---

## 8 · 已知不一致

### ✅ 免费额度 —— 已解决(08-21,owner 定为「首卦免费」)

曾经三处互相矛盾:服务端发 0 点 + 1 次免费解读,前端四处宣称 1,500 点,
而 `planDescription()` 里还写死着 500。**新用户被告知有 1,500 点,实际余额是 0。**

现在统一为**首卦免费**:`PLANS.free = { grant: 0, freeReadings: 1 }`,
所有文案从这张表读,不许再写死数字。

**为什么是一篇解读而不是一笔点数**:点数余额会在解读写到一半时用完,
而且第一次来的人根本不知道 1,500 算多算少 ——
一篇完整解读没有这个歧义,它会写完,而且**它就是产品本身,不是样品**。

⚠️ 根因值得记住:**同一个数字被抄了四遍。** 修一处只会让它和另外三处的某一处对齐,
不会让它对 —— 我就干过一次,把 settings 改成读客户端的表,而那张表本身是错的,
**修得更整齐,也更错**。

### 其它

- **21 个元素被 3–4 个样式表同时上色**(login `.card`、settings `.panel` 被四个)——
  这是每一次「改了没效果」的根源。
- 约 **9 处**真正裸写的颜色声明待清(`luxury-glass.css` 7 / `refinement.css` 2)。原来记的 47 处里,大部分是 `var(--token, #hex)` 兜底值和令牌定义 —— 都是合法写法;`poster-pages.css` 整份退役后又去掉一批。
- **CLAUDE.md §2 架构图过期**:把 `styles.css` 和 `prompt-engine.js` 列在仓库根目录,
  前者不存在,后者在 `functions/_lib/`。
- `liuyao-chart.js` 是死代码(没有任何调用),文件保留但 script 标签已移除。
- 色卡的 `pattern` / `harmony` / `families` / `breaks` 四个字段**运行时引用 0 次**。

---

## 9 · 三句话总结

1. **卦是算的,话是模型写的,两件事分开并且给用户看** —— 这是产品的立身之处。
2. **能算出来的就不要存成文件** —— 因为背景在跑 114 组配色,烘焙进文件的颜色跟不上。
3. **确定性不是随机** —— 手绘感、纹理、配色顺序全部是种子化的;
   逐次随机读起来是故障,不是设计。
