{
  "name": "bournewise",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node scripts/run-contracts.mjs",
    "dev": "npx serve . -l 3000 --no-clipboard",
    "start": "npx serve . -l 3000 --no-clipboard",
    "dev:fn": "wrangler pages dev . --port 8788",
    "deploy": "npx wrangler pages deploy .",
    "local": "node scripts/local.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
