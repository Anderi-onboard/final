# 补全包 20260914 —— 落在这里的是什么,以及它还差什么

`FINAL-六爻知识缺口补全包-20260914.zip` 原样落地(owner 2026-09-14 上传)。
**放在 `functions/_lib/` 下面是因为它是《增删卜易》原文加判据 —— 浏览器一个字都拿不到。**
这条界线和 `prompt-engine.js` 08-14 搬进 `_lib` 是同一条,`tests/prompt-secrecy.mjs` 守着它。

| 文件 | 条数 | 是什么 |
|---|---|---|
| `sop3-36.jsonl` | 36 | 动态演变的判据(「这笔生/克算不算数」),带 `规则表达式` 和 `依赖事实` |
| `sop5-10.jsonl` | 10 | 应期判据,只产候选,不改吉凶方向 |
| `exam-types-3.jsonl` | 3 | 考试的三个主机制类型(面试/笔试/升学/资格证是场景标签,不是类型) |
| `exam-rules-10.jsonl` | 10 | 考试六正四反,带 `base_weight` |
| `mappings-29.jsonl` | 29 | 象义映射卡(六亲/六神/爻位/神煞/…) |
| `six-relations-spirits-11.json` | 11 | 六亲 5 + 六神 6 |
| `linepos-shensha-18.json` | 18 | 爻位 6 + 神煞 12 |
| `gold-cases-12.jsonl` | 12 | 书页上已知答案的验例 |
| `feature-alias-map.json` | 34 | **`project_feature` 由这边填的那一份,见下** |

---

## 一、别名表:31 条接上了,3 条留 null

包里写着「当前包没有伪造 `project_feature`……字段绑定必须由代码方对照当前提交完成」。
这边照同一条规矩办:**填不上的留 null,不编。**
填一个不存在的检索键,那条判据永远命中不了 —— **而且不报错**。

| | 条数 | |
|---|---|---|
| `bound` | 24 | 仓库同名发得出来 |
| `bound_renamed` | 2 | 旺相/休囚死 → `TARGET_STRONG` / `TARGET_WEAK`(仓库只对**用神**发,对任意一爻发等于恒为真) |
| `bound_from_m1` | 5 | 不是盘上的事实,由 M1 从问题里读 |
| `blocked` | 3 | `MOVING_TOMB` · `OPEN_TOMB_BY_CLASH` · `AT_ABSOLUTE` |

那 3 条是**引擎真的算不出来**:动墓(入动爻之墓,和化墓不是一回事)、冲墓(冲开墓库)、
临绝(爻临自己的绝地,和「化绝」不是一回事)。要接就得先在
`liuyao-relations.js` 里把这三个事实算出来,不是在这张表上补个名字。

## ⭐⭐ 二、接这张表的时候发现的:映射表短了一截,引擎早就算出来了

对着 34 条核了一遍,**21 条在 `liuyao-features.js` 里没有落点** —— 而其中绝大多数
`liuyao-relations.js` **一直在算**:真空/假空/真破/假破/暗动/化空/化绝/回头冲/回头合
全在 `state` 和 `transforms[].kinds` 里,只是那张映射表没往外发。

也就是说:**46 条判据引用的检索键有三分之二找不到地方落,不是因为引擎读不出来,
是因为一张映射表短了一截。** 而一张没同步的映射表不会报错 —— 它只让下游永远
检索不到那一类卡,下游照样有卡可读。这是本仓库反复付钱的那个形状。

已补齐(`tests/feature-vocab.mjs` 守着不许再出孤儿):
- **真/假分开发**,不许合成一个 id:真空是「这条线死了」,假空是「它还在,只是现在不出面」——
  两条相反的断法路由到同一批卡上就是白路由。
- **三种墓各自也发自己的 id**:日墓待冲、月墓待出月、动墓随动爻,SOP-5 恰恰按这个分尺度。
- **「化绝」从 `AT_ABSOLUTE` 改名成 `TRANSFORM_ABSOLUTE`。** 那是两件事:
  临绝是「这一爻在绝地」,化绝是「它动了,动成了自己的绝」——
  后者是《增删卜易》§3 写死的大凶败局。混成一个 id,那条败局检索不到自己的卡,
  **而页面上什么都看不出来**。`AT_ABSOLUTE` 一直是这边起错的名字,包里的规范名是后者。

实测那副考试盘:补齐前发 15 个 feature,补齐后 17 个,新出来的两个正是
`TRANSFORM_ABSOLUTE` 和 `RETURN_CLASH` —— **就是这副盘上断错的那一条**。

## 三、还没做的,而且知道没做

按包里 `07_交付说明/接线顺序.md` 的六步,现在只走完了第 1 步的一半:

1. ✅ 别名表填好(31/34),❌ 3 条 blocked 要先在引擎里把事实算出来
2. ✅ **SOP-3 的 36 条在跑**(`criteria.js`):27 条判得了,9 条缺数据(长生十二宫、化散、近事、随鬼)。
   它们是**代码判据**,不是 RAG 召回卡。包里定的规矩:**同一关系命中「能」与「不能」时,
   取条件更具体者并记冲突** —— 2026-09-23 实现为 `resolve()`。
   ⚠️ 「更具体」包里没说怎么数。现在按「成立那条路上用了几个条件」比,一样多就不裁。
   这是一种读法,换读法只改 `resolve()` 一个函数。
3. ✅ SOP-5 的 10 条在跑(`timing.js`),八条算得出来,只产应期候选。
4. ❌ 考试三类型路由没接(现在 `CLASS-EXAM` 只有三张类型卡,十条判据没进来)。
5. 🟡 象义卡:29 + 11 + 18 已落盘,**还没进 RAG 的 `data.js`** —— 进去之前它们不参与检索。
6. ❌ 12 个金标例还没做成规则契约。包里那句要当真:
   **「引擎重建出的盘若与书页不一致,应先修排盘/历法,不应改规则迎合结果。」**

## 四、落地后改过的(2026-09-23,owner 说「先修吧」)

两处都改在 `sop3-36.jsonl` 里,每条记录带着 `本仓库改动` 字段(原表达式、原主体、为什么)。
要撤就照那个字段改回去。

| 条目 | 原来 | 现在 | 为什么 |
|---|---|---|---|
| 进神.1、.2 | `MOVING_LINE_STRONG AND TRANSFORMED_LINE_STRONG` 等 | 前面加 `ADVANCE_SPIRIT AND` | 依赖事实里写着化进神,同章另六条都带化进神/化退神,唯独这两条漏了 |
| 三墓五条 | 主体用神,`TARGET_…` | 主体世爻,`WORLD_…` | 原文「只验世爻入墓有三」,随鬼入墓章说的都是世爻 |

**进神那一处量过**:400 副盘(婚恋、考试、求财、工作各 100)上,进神.1、.2 成立 292 爻次,
那一爻真是化进神的只有 12 次。其余 280 次是动变旺衰恰好一样 —— 申化申、戌化戌这种原地没动的
伏吟爻也算了进去。修好之后,进神、退神成立的每一爻都真是化进神、化退神(`tests/criteria-engine.mjs` ⑥)。

**三墓那一处**,顺带修了 `criteria.js` 的一个洞:破墓原来读整张表(任一爻破墓就算),
主体一爻都没入墓也照样成立。现在只读主体自己那一爻。

## 必须保留的边界(原样抄自包里,别改)

- 46 条是**代码判据**,不是 RAG 的普通语义召回卡;RAG 只给模型提供对应原文与解释。
- `MULTIPLE_PLAUSIBLE_INTERPRETATIONS` 是**推理状态,不是事实检索键**
  (`liuyao-features.js` 的 `NOT_A_FACT` 已经这么写着)。
- 六亲、六神、神煞、爻位的现实象义卡**只作映射;不得借它们翻转主裁决**。
  主裁决归引擎和判据。
