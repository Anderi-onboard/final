/* The About route.
   Nothing to paint here any more. The content blocks are type only — the
   reference's own discipline: its text blocks carry no marks at all, and the
   marks live in the blocks that are nothing but marks. Combinations of motifs
   under a heading were what "the symbols do not mean anything together" was
   pointing at, and the fix is to not make meaningless combinations rather than
   to hunt for a meaning they never had. The two texture blocks are rendered by
   blocks.js from their data-scatter/data-lead attributes. */
